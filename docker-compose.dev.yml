name: bomberos-frontend-dev
services:
  frontend-app:
    build: .
    container_name: bomberos-frontend-dev
    env_file:
      - .env
    networks:
      - proxy_net
    restart: unless-stopped

networks:
  proxy_net:
    external: true