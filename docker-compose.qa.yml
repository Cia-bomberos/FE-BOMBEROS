name: bomberos-frontend-qa
services:
  frontend-app:
    build: .
    container_name: bomberos-frontend-qa
    env_file:
      - .env
    networks:
      - proxy_net
    restart: unless-stopped

networks:
  proxy_net:
    external: true